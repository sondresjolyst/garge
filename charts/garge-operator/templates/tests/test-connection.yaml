apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "garge-operator.fullname" . }}-test-connection"
  labels:
    {{- include "garge-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "garge-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
