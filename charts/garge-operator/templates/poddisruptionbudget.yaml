{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "garge-operator.fullname" . }}
  labels:
    {{- include "garge-operator.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "garge-operator.selectorLabels" . | nindent 6 }}
{{- end }}
