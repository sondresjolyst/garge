{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "garge-api.fullname" . }}
  labels:
    {{- include "garge-api.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "garge-api.selectorLabels" . | nindent 6 }}
{{- end }}
