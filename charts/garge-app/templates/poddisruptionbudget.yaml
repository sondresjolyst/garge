{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "garge-app.fullname" . }}
  labels:
    {{- include "garge-app.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "garge-app.selectorLabels" . | nindent 6 }}
{{- end }}
